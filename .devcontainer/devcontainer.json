{
  "name": "Flutter BLE Dev",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt update -y && sudo apt install -y git curl unzip xz-utils zip libglu1-mesa ninja-build libgtk-3-dev mesa-utils && echo 'export PATH=\"$PATH:/workspaces/esp32_ble_app/flutter/bin\"' >> ~/.bashrc && source ~/.bashrc",
  "customizations": {
    "vscode": {
      "extensions": [
        "dart-code.flutter",
        "dart-code.dart-code"
      ]
    }
  }
}